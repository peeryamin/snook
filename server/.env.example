PORT=8080
HOST=0.0.0.0
NODE_ENV=production
JWT_SECRET=CHANGE-ME-use-openssl-rand-base64-32
CORS_ORIGIN=http://localhost:8080

# SQLite path (Docker uses /data/parlor.db)
DB_PATH=

# Daily report email (optional)
REPORT_EMAIL_TO=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

DAILY_RESET_HOUR=23
DAILY_RESET_MINUTE=59
